version: "3.9"

services:
  api:
    build: .
    container_name: incident-tracker-api
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: ${DATABASE_URL:-sqlite+aiosqlite:///./incidents.db}
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

